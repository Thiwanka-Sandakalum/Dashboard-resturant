import { OrderTransformationPipe } from './order-transformation.pipe';

describe('OrderTransformationPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderTransformationPipe();
    expect(pipe).toBeTruthy();
  });
});
